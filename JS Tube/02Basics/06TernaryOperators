// show sign out button if user is already authenticated
// Otherwise show login/signup button

var authenticated = false;

authenticated ? console.log("Sign Out") : console.log("Sign In/Sign Up");